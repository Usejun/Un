fn fib(n: int) -> int
    if dp[n] == 0
        dp[n] = fib(n - 2) + fib(n - 1)
    -> dp[n]

dp = []

for k in range(3, 100)
    dp = array(0, k + 1)
    dp[0] = 1
    dp[1] = 1 

    write(`{k}번째 {fib(k)}`)    