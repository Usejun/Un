class point(x, y, z)

class vec3 : point
    fn __init__(x, y, z)
        self.x = x
        self.y = y
        self.z = z

    sum = fn() -> self.x + self.y + self.z

p1 = point(1, 2, 3)
vec = vec3(4, 5, 6)

write(p1, vec.sum())