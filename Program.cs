Console.InputEncoding = Process.Encoding;
Console.OutputEncoding = Process.Encoding;

//Process.Initialize(args[0]);
//Process.Run(args[1]);

Process.Initialize("../../../Code");
Process.Run("test.un");